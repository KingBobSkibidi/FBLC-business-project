SECRET_KEY=replace-this-with-a-long-random-string
DATABASE_URL=postgresql://username:password@host:5432/locally?sslmode=require

# Optional local-style variables instead of DATABASE_URL:
# PGHOST=localhost
# PGPORT=5432
# PGDATABASE=locally
# PGUSER=postgres
# PGPASSWORD=
